[Cursor] Fix production authentication and database issues

CRITICAL FIXES:
1. Authentication Flow Improvements:
   - Fixed "stuck on sign-in page" issue on second login
   - Enhanced auth cache clearing (more aggressive cleanup)
   - Fixed middleware session handling for production
   - Removed debug console.log statements from middleware
   - Improved sign-in page to redirect if already logged in

2. Security Improvements:
   - Removed test-login page for production security
   - Removed debug page from auth routes
   - Cleaned up debug/test links from sign-in page

3. Database Fixes (for Vercel log errors):
   - Created comprehensive DATABASE_FIX_GUIDE.md
   - Added SQL scripts to fix reservations table missing
   - Added SQL scripts to fix users.name column missing
   - Created safe migration approach without dropping tables

4. Auth Cache Enhancements:
   - More aggressive localStorage/sessionStorage cleanup
   - Added cookie clearing for Supabase auth tokens
   - Prevents stale session data between logins

WHAT THIS FIXES:
✅ Master user can now access facilities page on production
✅ Second login no longer gets stuck on sign-in page
✅ Removed security vulnerabilities (test/debug pages)
✅ Resolves PGRST200 error: "reservations and fields relationship"
✅ Resolves 42703 error: "users.name column missing"

DEPLOYMENT NOTES:
1. Apply database fixes via Supabase SQL Editor (see DATABASE_FIX_GUIDE.md)
2. Clear browser cache/cookies after deployment
3. Test with fresh browser session